/**
 * Apps directory full-screen push route — thin. Renders the feature screen from
 * features/plugins/. Routing only, no business logic.
 */
import { AppsScreen } from '@/features/plugins/AppsScreen';

export default function AppsRoute() {
  return <AppsScreen />;
}
